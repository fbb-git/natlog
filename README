Natlog is a utility logging traffic through a firewall that performs
source-NATting (a.k.a. POSTROUTING). 

Natlog's repository has moved to https://https://gitlab.com/fbb-git/natlog
and is not maintained at this location anymore.

The project's web-page can be found at http://fbb-git.gitlab.io/natlog/
